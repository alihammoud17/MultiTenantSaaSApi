# Copy this file to /etc/multitenant-saas-api/db.env before running Docker.
# Change every placeholder value before local runtime.
# Do NOT commit real passwords or production connection details.

# PostgreSQL container defaults for local Docker network
POSTGRES_DB=saasapi
POSTGRES_USER=saasapi_user
POSTGRES_PASSWORD=CHANGE_ME_DB_PASSWORD
