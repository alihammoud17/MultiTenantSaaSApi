# Copy this file to /etc/multitenant-saas-api/redis.env before running Docker.
# Change placeholder credentials/settings before local runtime if you enable auth.

# Redis container configuration for local Docker network
REDIS_HOST=redis
REDIS_PORT=6379
# Optional: set if running Redis with authentication
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD
